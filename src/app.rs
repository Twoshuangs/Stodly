pub struct App {
    pub timer: TimerState,
    pub app_time: AppTime,
    pub termusic: bool,
    pub stats: bool,
}

impl App {
    pub fn new() -> App {
        App {
            timer: 
        }
    }
